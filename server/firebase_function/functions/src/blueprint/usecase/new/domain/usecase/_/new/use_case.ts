class _NewUseCase {
    async case1(query: string): Promise<any> {
        // const result = await this.writeHereRepo.function1(query);
    }

    // constructor(private writeHereRepo: SomeRepoInterface) {}
}

export class NewUseCase {
    // static useCase = user _NewUseCase(user SomeRepo());
}

// usage
// NewUseCase.useCase.case1('some query');
